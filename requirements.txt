# Web dashboard (optional — required only with --ui flag)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Core async & networking
aiohttp>=3.9.0
websockets>=12.0

# Data processing
numpy>=1.26.0
pandas>=2.1.0
pyarrow>=14.0.0          # Parquet support for historical data
zstandard>=0.22.0        # Compressed data storage

# Optional: timezone support (for market hours guard)
tzdata>=2024.1            # Required on Windows for zoneinfo

# Optional broker libraries (install the one matching your provider)
# NorenRestApiPy>=1.0.20     # Shoonya / Finvasia — pip install NorenRestApiPy
# smartapi-python>=1.3.4     # Angel One      — pip install smartapi-python
# dhanhq>=2.0.0              # Dhan HQ        — pip install dhanhq
# fyers-apiv3>=3.1.0         # Fyers API v3   — pip install fyers-apiv3
# upstox-python-sdk>=2.0.0   # Upstox API v2  — pip install upstox-python-sdk

# TOTP (for Angel One / Fyers 2FA)
pyotp>=2.9.0

# Dev / testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
